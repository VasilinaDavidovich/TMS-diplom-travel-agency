# 🏨 Star Travel - Туристическое агентство

Веб-приложение для бронирования отелей на Django.


## 🚀 Возможности

- 🔐 Регистрация и авторизация пользователей
- 🏨 Просмотр списка отелей с фильтрацией и сортировкой
- 🏆 Система отзывов и рейтингов
- 📅 Бронирование номеров
- ❤️ Добавление отелей в избранное
- 🔍 Поиск отелей по странам и городам
- 🏠 Личный кабинет пользователя


## 🛠 Технологии

- **Backend**: Django 4.2 + Django REST Framework
- **Database**: PostgreSQL
- **Authentication**: JWT (JSON Web Tokens)
- **Frontend**: HTML, CSS, JavaScript, Bootstrap 5
- **Containerization**: Docker + Docker Compose
- **Testing**: Django Test Framework


## 📦 Установка и запуск


### С помощью Docker (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <url-репозитория>
cd star-travel
```

2. Настройте окружение:
# Скопируйте пример настроек
cp .env.example .env
# Отредактируйте .env файл, указав свои настройки базы данных

3. Запустите приложение:
```bash
docker-compose up --build
```

4. Приложение автоматически:
- ✅ Создаст базу данных PostgreSQL
- ✅ Применит миграции
- ✅ Загрузит демо-данные (страны, города, отели)
- ✅ Запустит сервер

5. Приложение будет доступно по адресу: http://localhost:8000
Демо-данные включают:
- 🏨 15+ отелей в Европе
- 🌍 5 стран (Австрия, Венгрия, Италия, Чехия, Нидерланды)
- 🏙️ 10+ городов

6. Для доступа в админку:
```bash
docker-compose exec web python manage.py createsuperuser
```

### Без Docker

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте базу данных PostgreSQL


3. Выполните миграции:
```bash
python manage.py migrate
```

4. Загрузите демо-данные:
```bash
python manage.py loaddata fixtures/countries.json fixtures/cities.json fixtures/hotels.json
```

5. Создайте суперпользователя:
```bash
python manage.py createsuperuser
```

6. Запустите сервер:
```bash
python manage.py runserver
```


## 🗄 Модели данных

- **Country** - Страны
- **City** - Города  
- **Hotel** - Отели
- **HotelImage** - Изображения отелей
- **Review** - Отзывы
- **Booking** - Бронирования
- **Favorite** - Избранные отели


### 🔧 Основные API Endpoints

#### Аутентификация (accounts)
- `POST /api/auth/api/register/` - Регистрация
- `POST /api/auth/api/login/` - Вход в систему
- `GET /api/auth/api/current-user/` - Данные текущего пользователя
- `DELETE /api/auth/api/delete-account/` - Удаление аккаунта

#### Отели (hotels) - RESTful
- `GET /api/hotels/` - Список отелей с фильтрацией
- `GET /api/hotels/{id}/` - Детали отеля

#### Отзывы - RESTful  
- `GET /api/reviews/` - Все отзывы
- `GET /api/reviews/?user=username` - Отзывы конкретного пользователя
- `POST /api/reviews/` - Создать отзыв
- `GET /api/reviews/{id}/` - Получить конкретный отзыв
- `DELETE /api/reviews/{id}/` - Удалить отзыв

#### Бронирования - RESTful
- `GET /api/bookings/` - Бронирования пользователя  
- `POST /api/bookings/` - Создать бронирование
- `GET /api/bookings/{id}/` - Получить конкретное бронирование
- `DELETE /api/bookings/{id}/` - Удалить бронирование

#### Избранное - RESTful
- `GET /api/favorites/` - Список избранного
- `POST /api/favorites/` - Добавить в избранное
- `GET /api/favorites/{id}/` - Получить конкретное избранное
- `DELETE /api/favorites/{id}/` - Удалить из избранного

#### Справочники
- `GET /api/countries/` - Список стран
- `GET /api/cities/` - Список городов


## 🧪 Тестирование

Запуск тестов:
```bash
docker-compose exec web python manage.py test
```


## 📁 Структура проекта
VTour/
│─ accounts/              # Приложение аутентификации
│   ├── api/              # API аутентификации
│   │   ├── urls.py       # API маршруты аутентификации
│   │   └── views.py      # API представления аутентификации
│   ├── serializers.py    # Сериализаторы пользователей
│   ├── urls.py           # Frontend маршруты аутентификации
│   └── views.py          # Frontend представления аутентификации
├── backend/              # Настройки Django
├── fixtures/             # Демо-данные
│   ├── countries.json
│   ├── cities.json
│   ├── hotels.json
│   └── ...
├── hotels/               # Приложение отелей
│   ├── api/              # API отелей
│   │   ├── urls.py       # API маршруты отелей
│   │   └── views.py      # API представления отелей
│   ├── migrations/       # Миграции базы данных
│   ├── templates/        # HTML шаблоны
│   ├── models.py         # Модели данных
│   ├── serializers.py    # Сериализаторы API 
│   ├── tests.py          # Тесты
│   ├── urls.py           # Frontend URL маршруты 
│   └── views.py          # Frontend представления
├── .env.example          # Пример настроек окружения
├── docker-compose.yml    # Docker конфигурация
├── Dockerfile            # Docker образ
├── README.md             # Документация
└── requirements.txt      # Зависимости Python


## 👨‍💻 Разработка

### Миграции базы данных
```bash
python manage.py makemigrations
python manage.py migrate
```


### Создание суперпользователя
```bash
python manage.py createsuperuser
```

### Работа с фикстурами
```bash
# Создание фикстур
python manage.py dumpdata --indent 2 accounts.CustomUser > fixtures/users.json
python manage.py dumpdata --indent 2 hotels > fixtures/hotels_data.json

# Загрузка фикстур
python manage.py loaddata fixtures/filename.json
```

## 📄 Лицензия

MIT License